<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
        IMPORTANT:
        We keep *common* theme items in an unqualified style so both Light and Night
        share the same setup.
        Then, in values-night/themes.xml we only override the few Night-specific items
        (e.g., status bar icon color) WITHOUT breaking the whole Material3 theme.
    -->

    <style name="Base.Theme.CafeManagementServices.Common" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>

        <item name="android:windowBackground">@color/app_background</item>
        <item name="colorSurface">@color/app_surface</item>

        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>

        <item name="android:statusBarColor">@color/app_background</item>
        <item name="android:navigationBarColor">@color/app_background</item>

        <item name="textInputStyle">@style/Widget.Cafe.TextInput</item>
        <item name="android:editTextStyle">@style/Widget.Cafe.EditText</item>
    </style>

    <!-- Light variant (default) -->
    <style name="Base.Theme.CafeManagementServices" parent="Base.Theme.CafeManagementServices.Common">
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.CafeManagementServices" parent="Base.Theme.CafeManagementServices" />
    <style name="Theme.CafeManagementServices.Auth" parent="Base.Theme.CafeManagementServices" />

    <style name="Widget.Cafe.TextInput" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxBackgroundColor">?attr/colorSurface</item>
        <item name="boxStrokeColor">?attr/colorPrimary</item>
        <item name="hintTextColor">?attr/colorOnSurfaceVariant</item>
        <item name="endIconTint">?attr/colorOnSurfaceVariant</item>

        <item name="boxCornerRadiusTopStart">16dp</item>
        <item name="boxCornerRadiusTopEnd">16dp</item>
        <item name="boxCornerRadiusBottomStart">16dp</item>
        <item name="boxCornerRadiusBottomEnd">16dp</item>
    </style>

    <style name="Widget.Cafe.TextInputEditText" parent="Widget.Cafe.EditText">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Buttons -->
    <style name="Widget.Cafe.PrimaryButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">18dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="Widget.Cafe.OutlinedButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="strokeColor">?attr/colorOnSurfaceVariant</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">18dp</item>
        <item name="android:minHeight">48dp</item>
    </style>
    <style name="Widget.Cafe.EditText" parent="@android:style/Widget.EditText">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
    </style>
    <style name="Base.Theme" parent="Base.Theme.CafeManagementServices" />

</resources>
